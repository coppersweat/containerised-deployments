# Run an R script that updates data on a database every minute
* * * * * root /usr/local/bin/Rscript --no-save --verbose /root/update-data.R 2>&1 | tee -a /var/log/rscript/update-data.Rout
# An empty line is required at the end of this file for a valid cron file.